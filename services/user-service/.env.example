# Environment
NODE_ENV=development
PORT=3002

# Database Configuration (for local development)
DB_HOST=localhost
DB_PORT=5433
DB_NAME=user_db
DB_USER=user_user
DB_PASSWORD=user_password

# Service URLs (for local development)
AUTH_SERVICE_URL=http://localhost:3001

# Encryption key for sensitive user data (ONLY example; replace in production)
# In production, use a strong random value from a secret manager, not this example.
DATA_ENCRYPTION_KEY=change-this-key-in-production
