# Быстрый старт 🚀

## Выбор пакетного менеджера

**Рекомендация: используйте `npm`** (стандарт Node.js, уже используется в Dockerfile'ах)

Альтернативы:
- `yarn` - быстрее в некоторых случаях
- `pnpm` - эффективнее по дисковому пространству

**Важно:** В Docker контейнерах всегда используется `npm`, локально можете использовать любой.

## Первый запуск (1 команда!)

```bash
docker-compose up --build
```

**Всё!** Docker автоматически:
- ✅ Создаст все базы данных PostgreSQL
- ✅ Установит зависимости
- ✅ Запустит миграции
- ✅ Запустит все сервисы

## Что будет доступно

После запуска (через 1-2 минуты):

- 🌐 **Frontend**: http://localhost:3000
- 🔐 **Auth Service**: http://localhost:3001
- 👤 **User Service**: http://localhost:3002
- 🍕 **Product Service**: http://localhost:3003
- 📦 **Order Service**: http://localhost:3004

## Проверка работоспособности

```bash
# Health checks
curl http://localhost:3001/health
curl http://localhost:3002/health
curl http://localhost:3003/health
curl http://localhost:3004/health
```

## Полезные команды

```bash
# Запуск в фоне
docker-compose up --build -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down

# Полная очистка (удалит БД!)
docker-compose down -v
```

## Проблемы?

См. подробную инструкцию: [GETTING_STARTED.md](./GETTING_STARTED.md)

