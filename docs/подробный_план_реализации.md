# Подробный план реализации проекта ГИГАПОИСК

> **📊 Статус проекта:** Фаза 1 ✅ (100%) | Фаза 2 ✅ (100%) | Фаза 3 ⏳ (0%)  
> **📅 Последнее обновление:** 14 января 2026  
> **📄 Подробный статус:** [статус_реализации.md](./статус_реализации.md)

## 🎯 Общая стратегия

**Принцип:** Сначала независимые сервисы, затем зависимые. Сначала инфраструктура, затем логика.

---

## 📊 Граф зависимостей сервисов

```
API Gateway (Next.js)
    │
    ├─→ Auth Service ──┐
    │                  │
    ├─→ User Service ──┼─→ Order Service ──┐
    │                  │                    │
    ├─→ Location Service ──┐               │
    │                      │               │
    ├─→ Provider Service ──┼─→ Availability Service ──┼─→ Order Service
    │                      │               │
    ├─→ Equipment Service ─┘               │
    │                                      │
    └─→ Notification Service ←─────────────┘
```

---

## 🔄 Последовательность реализации

### ✅ ФАЗА 1: Инфраструктура (Независимые сервисы) - ВЫПОЛНЕНО

#### Шаг 1.1: Создание структуры базовых сервисов

**Цель:** Создать "скелет" всех сервисов, чтобы они запускались

**Действия:**

1. Переименовать `application-service` → `order-service`
2. Создать структуру новых сервисов:
   - `location-service` (папка + базовые файлы)
   - `availability-service` (папка + базовые файлы)
   - `equipment-service` (папка + базовые файлы)
   - `notification-service` (папка + базовые файлы)

**Что создается в каждом:**

- `package.json`, `tsconfig.json`, `Dockerfile`
- `src/index.ts` (базовый Express сервер)
- `src/config/database.ts` (конфигурация БД)
- Структура папок: `models/`, `controllers/`, `routes/`, `services/`, `middleware/`

**Результат:** Все сервисы запускаются, но ничего не делают

---

#### Шаг 1.2: Настройка баз данных в docker-compose.yml

**Цель:** Все БД и сервисы добавлены в docker-compose

**Действия:**

1. Переименовать `application-db` → `orders-db` в docker-compose.yml
2. Добавить новые БД:
   - `locations-db` (порт 5436)
   - `availability-db` (порт 5437)
   - `equipment-db` (порт 5438)
   - `notifications-db` (порт 5439, опционально)
3. Добавить новые сервисы в docker-compose.yml:
   - `location-service` (порт 3005)
   - `availability-service` (порт 3006)
   - `equipment-service` (порт 3007)
   - `notification-service` (порт 3008)
4. Настроить `depends_on` для зависимостей

**Результат:** `docker-compose up` запускает все сервисы и БД

---

### ✅ ФАЗА 2: Независимые сервисы (можно делать параллельно) - ЗАВЕРШЕНО (100%)

#### Шаг 2.1: Location Service ✅ **ЗАВЕРШЕНО**

**Зависимости:** Нет (независимый)

**Статус:** ✅ Полностью реализован

**Последовательность:**

1. ✅ Создать модели Sequelize:
   - `Region` (регионы)
   - `City` (города, связь с Region)
   - `StreetType` (типы улиц: улица, проспект, переулок, бульвар, площадь, набережная, шоссе, тупик и т.д.)
   - `Street` (улицы, связь с City и StreetType)
   - `Building` (дома, связь с Street)
   - `Apartment` (квартиры, связь с Building)
2. ✅ Написать миграции БД (6 миграций созданы и применены)
3. ✅ Реализовать контроллеры:
   - `GET /api/locations/regions` (список регионов)
   - `GET /api/locations/cities?region_id={id}` (города по региону)
   - `GET /api/locations/street-types` (типы улиц: улица, проспект, переулок и т.д.)
   - `GET /api/locations/streets?city_id={id}&street_type_id={id}` (улицы по городу, с фильтром по типу)
   - `GET /api/locations/buildings?street_id={id}` (дома по улице)
   - `GET /api/locations/apartments?building_id={id}` (квартиры по дому)
   - `GET /api/locations/search?q={query}` (поиск по адресу)
   - `GET /api/locations/autocomplete?q={query}` (автодополнение адреса)
4. ✅ Реализовать сервисы (бизнес-логика)
   - ✅ LocationService - работа с БД
   - ✅ GeocoderService - интеграция с Яндекс Геокодером API
5. ✅ Добавить API routes в Next.js (`frontend/src/app/api/locations/route.ts`)

**Результат:** ✅ Можно искать адреса через API

**Дополнительно реализовано:**

- ✅ Интеграция с Яндекс Геокодером API для автодополнения адресов
- ✅ Правильная типизация TypeScript для ассоциаций Sequelize
- ✅ Обработка ошибок и логирование
- ✅ Ключ API настраивается через переменную окружения `YANDEX_GEOCODER_API_KEY`

**Используется в:**

- Availability Service (для проверки доступности по адресу)
- Order Service (для сохранения адреса в заявке)
- Frontend (форма поиска адреса)

---

#### Шаг 2.2: Provider Service ✅ **ЗАВЕРШЕНО**

**Зависимости:** Нет (независимый)

**Статус:** ✅ Полностью реализован и доработан

**Последовательность:**

1. ✅ Проверены существующие модели:
   - `Provider` (провайдеры)
   - `Tariff` (тарифные планы)
   - `Coverage` (покрытие)
2. ✅ Реализована фильтрация тарифов:
   - По типу услуги (интернет, ТВ, мобильная связь) - через параметр `serviceType`
   - По провайдеру - через параметр `providerId`
   - По цене - через параметры `minPrice`, `maxPrice`
   - По скорости - через параметр `speed`
   - По технологии - через параметр `technology`
3. ✅ Реализована сортировка:
   - По цене (`sortBy=price`)
   - По скорости (`sortBy=speed`)
   - По популярности (`sortBy=popularity`) - рейтинг \* количество отзывов
   - Порядок сортировки: `sortOrder=asc` или `sortOrder=desc`
4. ✅ Обновлены API endpoints:
   - `GET /api/tariffs` - с поддержкой всех фильтров и сортировки

**Результат:** ✅ Полноценный API для провайдеров и тарифов с фильтрацией и сортировкой

**Используется в:**

- Availability Service (для определения доступных провайдеров)
- Equipment Service (для связи оборудования с провайдерами)
- Order Service (для выбора тарифа в заявке)
- Frontend (список тарифов, фильтры)

---

#### Шаг 2.3: Equipment Service ✅ **ЗАВЕРШЕНО**

**Зависимости:** Provider Service (для связи оборудования с провайдерами)

**Статус:** ✅ Полностью реализован

**Последовательность:**

1. ✅ Созданы модели Sequelize:
   - `EquipmentType` (типы: роутер, ТВ-приставка, SIM-карта)
   - `Equipment` (оборудование, связь с Provider и EquipmentType)
   - Поля: `purchasePrice`, `installmentMonths`, `rentalMonthlyPrice`, `setupPrice`
2. ✅ Написаны миграции БД (2 миграции)
3. ✅ Реализованы контроллеры:
   - `GET /api/equipment` (список оборудования с фильтрами)
   - `GET /api/equipment/:id` (детали оборудования)
   - `GET /api/equipment/by-provider/:provider_id` (оборудование провайдера)
   - `GET /api/equipment/types` (типы оборудования)
   - `GET /api/equipment/types/:id` (тип оборудования по ID)
   - `POST /api/equipment` (создание оборудования)
   - `PUT /api/equipment/:id` (обновление оборудования)
   - `DELETE /api/equipment/:id` (удаление оборудования)
4. ✅ Реализованы сервисы:
   - `EquipmentService` - полная бизнес-логика
   - Интеграция с Provider Service для проверки существования провайдеров
5. ✅ Добавлены API routes в Next.js (`frontend/src/app/api/equipment/route.ts`)

**Результат:** ✅ API для получения оборудования провайдеров

**Используется в:**

- Order Service (для расчета стоимости оборудования в заявке)
- Frontend (выбор оборудования в форме заявки)

---

#### Шаг 2.4: Auth Service ✅ **ПРОВЕРЕН**

**Зависимости:** Нет (независимый)

**Статус:** ✅ Проверен, работает корректно

**Реализовано:**

- ✅ Модели: `User`, `RefreshToken`, `UserSession`, `VerificationCode` (все реализованы)
- ✅ Миграции БД (4 миграции)
- ✅ API endpoints: регистрация, вход, выход, обновление токена
- ✅ JWT токены работают (access token + refresh token)
- ✅ Интеграция с User Service

**Используется в:**

- Все сервисы (для проверки аутентификации)
- User Service (для получения user_id)
- Order Service (для привязки заявки к пользователю)

---

#### Шаг 2.5: User Service ✅ **ОБНОВЛЕН**

**Зависимости:** Auth Service (для получения user_id)

**Статус:** ✅ Обновлен - только для операторов/админов

**Реализовано:**

- ✅ Модели: `UserProfile`, `FavoriteTariff`, `UserPreference`, `UserActivity` (все реализованы)
- ✅ Миграции БД (7 миграций, включая новые модели)
- ✅ API endpoints: только для операторов (требуют авторизации)
  - ✅ `GET /api/users/profile` - получить свой профиль оператора
- ✅ Интеграция с Auth Service
- ✅ **Важно:** Удалены публичные endpoints для обычных пользователей

**Особенности:**

- **Только для операторов/админов CRM панели**
- Обычные пользователи НЕ используют User Service
- Данные обычных пользователей хранятся только в Order Service
- Оператор видит данные пользователя из заявки в CRM
- Только оператор может изменять данные через CRM панель

**Используется в:**

- CRM панель (для профилей операторов)
- НЕ используется в основном приложении (ГИГАПОИСК)

---

### ФАЗА 3: Зависимые сервисы (требуют другие сервисы)

#### Шаг 3.1: Availability Service

**Зависимости:**

- Location Service (для получения адреса)
- Provider Service (для получения списка провайдеров)

**Последовательность:**

1. Создать модели Sequelize:
   - `TechnicalAccess` (связь адреса с провайдером)
     - Поля: `building_id`, `apartment_id`, `provider_id`, `connection_type`, `is_available`
   - `AvailabilityCache` (кеш результатов проверки)
     - Поля: `address_hash`, `available_providers` (JSONB), `expires_at`
2. Написать миграции БД
3. Реализовать контроллеры:
   - `POST /api/availability/check` (проверка доступности по адресу)
   - `GET /api/availability/:address_id` (доступность по адресу)
   - `GET /api/availability/providers/:address_id` (доступные провайдеры)
4. Реализовать сервисы:
   - Метод проверки доступности (запрос к Location + Provider)
   - Кеширование результатов (Redis или БД)
   - Интеграция с внешними API провайдеров (если нужно)
5. Добавить API routes в Next.js

**Результат:** Можно проверить, какие провайдеры доступны по адресу

**Используется в:**

- Order Service (для проверки доступности при создании заявки)
- Frontend (форма поиска провайдеров, шаг проверки доступности)

---

#### Шаг 3.2: Order Service (переименовать и доработать Application Service)

**Зависимости:**

- Auth Service (для user_id)
- User Service (для данных пользователя)
- Location Service (для адреса)
- Provider Service (для тарифа)
- Equipment Service (для расчета стоимости оборудования)
- Availability Service (для проверки доступности)

**Последовательность:**

1. Переименовать сервис:
   - Папка: `application-service` → `order-service`
   - В docker-compose.yml: `application-service` → `order-service`
   - БД: `application_db` → `orders_db`
2. Обновить модели Sequelize:
   - `Order` (заявка):
     - Адрес: `region_id`, `city_id`, `street_id`, `building_id`, `apartment_id`
     - Контакты: `first_name`, `last_name`, `phone`, `email`, `date_of_birth`, `citizenship`
     - Услуги: `selected_tariff_id`, `selected_provider_id`
     - Оборудование: `router_option`, `router_price`, `tv_settop_option`, `sim_card_option`
     - Стоимость: `total_monthly_price`, `total_connection_price`, `total_equipment_price`
   - `OrderItem` (элементы заявки)
   - `OrderStatusHistory` (история статусов)
3. Написать миграции БД
4. Реализовать контроллеры:
   - `POST /api/orders` (создание заявки)
   - `GET /api/orders/:id` (получение заявки)
   - `GET /api/orders/user/:user_id` (заявки пользователя)
   - `PUT /api/orders/:id/status` (обновление статуса)
   - `GET /api/orders/:id/calculate` (расчет стоимости)
5. Реализовать сервисы:
   - Метод создания заявки:
     1. Проверить доступность (Availability Service)
     2. Получить данные тарифа (Provider Service)
     3. Получить цены оборудования (Equipment Service)
     4. Рассчитать стоимость
     5. Сохранить заявку
   - Метод расчета стоимости (тариф + оборудование)
6. Добавить API routes в Next.js
7. Настроить переменные окружения:
   - `LOCATION_SERVICE_URL`
   - `PROVIDER_SERVICE_URL`
   - `EQUIPMENT_SERVICE_URL`
   - `AVAILABILITY_SERVICE_URL`

**Результат:** Можно создавать заявки на подключение

**Используется в:**

- Notification Service (для отправки уведомлений о статусах)
- Frontend (форма заявки, страница статуса)
- CRM Service (для отображения заявок в CRM)

---

#### Шаг 3.3: Notification Service

**Зависимости:** Order Service (для получения данных о заявках)

**Последовательность:**

1. Создать модели Sequelize (опционально, можно без БД):
   - `Notification` (уведомления)
   - `NotificationTemplate` (шаблоны)
2. Написать миграции БД (опционально)
3. Реализовать контроллеры:
   - `POST /api/notifications` (отправить уведомление)
   - `GET /api/notifications/user/:user_id` (уведомления пользователя)
4. Реализовать сервисы:
   - Метод отправки email (nodemailer или сервис)
   - Шаблоны уведомлений:
     - Заявка создана
     - Статус заявки изменен
     - Заявка подтверждена
5. Интеграция с Order Service:
   - Подписка на события (или HTTP вызовы)
   - Отправка уведомлений при изменении статуса
6. Добавить API routes в Next.js

**Результат:** Пользователи получают email уведомления о статусах заявок

**Используется в:**

- Order Service (вызывает при изменении статуса)
- Frontend (отображение уведомлений)

---

### ФАЗА 4: Frontend основного приложения

#### Шаг 4.1: API Gateway в Next.js

**Зависимости:** Все сервисы

**Последовательность:**

1. Создать API routes для каждого сервиса:
   - `frontend/src/app/api/locations/route.ts`
   - `frontend/src/app/api/availability/route.ts`
   - `frontend/src/app/api/providers/route.ts`
   - `frontend/src/app/api/equipment/route.ts`
   - `frontend/src/app/api/orders/route.ts`
   - `frontend/src/app/api/notifications/route.ts`
2. Настроить проксирование запросов:
   - Каждый route делает HTTP запрос к соответствующему микросервису
   - Обработка ошибок
   - Логирование
3. Настроить CORS и rate limiting
4. Настроить переменные окружения для URL сервисов

**Результат:** Frontend обращается к `/api/*`, запросы проксируются к микросервисам

---

#### Шаг 4.2: Базовые страницы Frontend

**Зависимости:** API Gateway

**Последовательность:**

1. Главная страница (`/`):
   - Форма поиска по адресу
   - Интеграция с Location Service API
   - Автодополнение адресов
2. Страница поиска провайдеров (`/search`):
   - Многошаговая форма:
     - Шаг 1: Тип подключения
     - Шаг 2: Адрес (Location Service)
     - Шаг 3: Проверка доступности (Availability Service)
     - Шаг 4: Результаты (список доступных провайдеров)
3. Страница списка тарифов (`/tariffs`):
   - Фильтры (Provider Service)
   - Сортировка
   - Карточки тарифов
   - Избранное (User Service)
4. Страница деталей тарифа (`/tariffs/[id]`):
   - Полная информация о тарифе
   - Кнопка "Подключение"

**Результат:** Пользователь может искать провайдеров и просматривать тарифы

---

#### Шаг 4.3: Форма заявки (Order Wizard)

**Зависимости:** Все сервисы

**Последовательность:**

1. Создать многошаговый wizard (`/order`):
   - **Шаг 1:** Тип подключения (квартира/частный сектор/офис)
   - **Шаг 2:** Адрес (Location Service)
   - **Шаг 3:** Проверка доступности (Availability Service)
   - **Шаг 4:** Фильтрация услуг (интернет/ТВ/связь)
   - **Шаг 5:** Сортировка тарифов (цена/скорость)
   - **Шаг 6:** Выбор провайдера (Provider Service)
   - **Шаг 7:** Выбор тарифа (Provider Service)
   - **Шаг 8:** Роутер (Equipment Service)
   - **Шаг 9:** ТВ-приставка (Equipment Service)
   - **Шаг 10:** SIM-карта (Equipment Service)
   - **Шаг 11:** Личные данные (форма)
   - **Шаг 12:** Подтверждение (расчет стоимости)
   - **Шаг 13:** Результат (Order Service - создание заявки)
2. Валидация форм на каждом шаге
3. Сохранение состояния формы (localStorage или state)
4. Обработка ошибок
5. Интеграция с Order Service API

**Результат:** Пользователь может оформить заявку на подключение

---

#### Шаг 4.4: Страница статуса заявки

**Зависимости:** Order Service, Notification Service

**Последовательность:**

1. Страница `/order/[id]`:
   - Отображение номера заявки
   - Текущий статус
   - История статусов (OrderStatusHistory)
   - Детали заявки (адрес, тариф, оборудование, стоимость)
2. Действия:
   - Скриншот заявки (File Service, если есть)
   - Назначение времени звонка (Scheduling Service, если есть)
3. Интеграция с Order Service API

**Результат:** Пользователь может отслеживать статус заявки

---

### ФАЗА 5: Рекомендуемые сервисы (можно делать параллельно)

#### Шаг 5.1: Scheduling Service

**Зависимости:** Order Service, User Service

**Последовательность:**

1. Создать структуру сервиса
2. Модели: `Appointment`, `OperatorSchedule`, `TimeSlot`
3. API: назначение времени звонка оператора
4. Интеграция с Order Service

**Используется в:** Order Service, Frontend (назначение времени)

---

#### Шаг 5.2: Consultation Service

**Зависимости:** User Service

**Последовательность:**

1. Создать структуру сервиса
2. Модели: `ConsultationRequest`, `ConsultationChannel`
3. API: запросы на консультацию (Max, Telegram, звонок)

**Используется в:** Frontend (форма консультации)

---

#### Шаг 5.3: Integration Service

**Зависимости:** Provider Service, Availability Service

**Последовательность:**

1. Создать структуру сервиса
2. Интеграция с внешними API провайдеров
3. Webhook обработка
4. Кеширование данных

**Используется в:** Availability Service (для проверки доступности)

---

#### Шаг 5.4: File Service

**Зависимости:** Order Service

**Последовательность:**

1. Создать структуру сервиса
2. Загрузка файлов (логотипы провайдеров, документы)
3. Генерация скриншотов заявок

**Используется в:** Provider Service (логотипы), Order Service (скриншоты)

---

### ФАЗА 6: CRM панель

#### Шаг 6.1: CRM Service

**Зависимости:** Order Service, Workflow Service

**Последовательность:**

1. Создать структуру сервиса
2. Модели: `OrderView`, `OrderAssignment`, `OrderCrmHistory`
3. API: просмотр заявок, фильтрация, поиск, назначение операторов
4. Интеграция с Order Service (чтение данных)

**Используется в:** CRM Frontend

---

#### Шаг 6.2: Workflow Service

**Зависимости:** Order Service

**Последовательность:**

1. Создать структуру сервиса
2. Модели: `WorkflowStage`, `WorkflowTransition`, `WorkflowRule`
3. Этапы: Формирование, Наведение, Подключение, Конкретизирование, Вознаграждение, Дублирование, Архивирование
4. API: переходы между этапами, правила автоматических переходов
5. Интеграция с Order Service

**Используется в:** CRM Service, CRM Frontend

---

#### Шаг 6.3: Admin Service

**Зависимости:** Provider Service, Location Service, Equipment Service

**Последовательность:**

1. Создать структуру сервиса
2. Модели: `AdminUser`, `AdminAction`, `SystemSetting`
3. API: управление локациями, провайдерами, тарифами, оборудованием, операторами
4. Интеграция с другими сервисами (CRUD операции)

**Используется в:** CRM Frontend (админ-панель)

---

#### Шаг 6.4: Frontend CRM панели

**Зависимости:** CRM Service, Workflow Service, Admin Service

**Последовательность:**

1. Создать Next.js проект для CRM
2. Layout (сайдбар, хедер)
3. Страница списка заявок (фильтры, поиск)
4. Страница деталей заявки
5. Страницы управления (локации, провайдеры, тарифы, оборудование)
6. Страница управления операторами
7. Страница настроек системы

**Результат:** Операторы могут управлять заявками и контентом

---

### ФАЗА 7: Синхронизация

#### Шаг 7.1: Event-Driven Architecture

**Зависимости:** Все сервисы

**Последовательность:**

1. Настроить Message Broker (RabbitMQ/Kafka)
2. Реализовать события от основного приложения:
   - `order.created`
   - `order.status.changed`
3. Реализовать события от CRM:
   - `order.status.updated`
   - `order.stage.changed`
   - `provider.updated`
   - `tariff.updated`
4. Event handlers в сервисах
5. Webhooks для критичных обновлений
6. Мониторинг событий

**Результат:** Автоматическая синхронизация между основным приложением и CRM

---

## 📋 Чеклист по порядку

### ✅ Фаза 1: Инфраструктура - ВЫПОЛНЕНО

- [x] Переименовать application-service → order-service
- [x] Создать структуру location-service
- [x] Создать структуру availability-service
- [x] Создать структуру equipment-service
- [x] Создать структуру notification-service
- [x] Обновить docker-compose.yml (БД + сервисы)

### ✅ Фаза 2: Независимые сервисы - ЗАВЕРШЕНО (100%)

- [x] Location Service (модели, миграции, API) ✅ **ЗАВЕРШЕНО**
  - [x] Модели Sequelize: Region, City, StreetType, Street, Building, Apartment
  - [x] Миграции БД (6 миграций)
  - [x] Контроллеры (8 endpoints)
  - [x] Сервисы: LocationService, GeocoderService
  - [x] Интеграция с Яндекс Геокодером API
  - [x] API routes в Next.js
  - [x] Типизация TypeScript исправлена
- [x] Provider Service (фильтрация/сортировка) ✅ **ЗАВЕРШЕНО**
  - [x] Фильтрация по типу услуги (internet, tv, mobile)
  - [x] Фильтрация по провайдеру, цене, скорости, технологии
  - [x] Сортировка по цене, скорости, популярности
  - [x] Обновлены контроллеры и сервисы
- [x] Equipment Service (модели, миграции, API) ✅ **ЗАВЕРШЕНО**
  - [x] Модели Sequelize: EquipmentType, Equipment
  - [x] Миграции БД (2 миграции)
  - [x] Контроллеры (8 endpoints)
  - [x] Сервисы: EquipmentService
  - [x] Интеграция с Provider Service
  - [x] API routes в Next.js
- [x] Auth Service ✅ **ЗАВЕРШЕНО**
  - [x] Модели: User, RefreshToken, UserSession, VerificationCode
  - [x] Миграции БД (4 миграции)
  - [x] API endpoints: регистрация, вход, выход, обновление токена
  - [x] JWT токены работают
- [x] User Service ✅ **ЗАВЕРШЕНО**
  - [x] Модели: UserProfile, FavoriteTariff, UserPreference, UserActivity
  - [x] Миграции БД (7 миграций)
  - [x] API endpoints: профиль по телефону
  - [x] Интеграция с Auth Service

### ✅ Фаза 3: Зависимые сервисы

- [ ] Availability Service (модели, миграции, API, интеграция)
- [ ] Order Service (переименовать, доработать модели, API, интеграция)
- [ ] Notification Service (модели, API, интеграция)

### ✅ Фаза 4: Frontend

- [ ] API Gateway (проксирование)
- [ ] Базовые страницы (главная, поиск, тарифы)
- [ ] Форма заявки (wizard)
- [ ] Страница статуса заявки

### ✅ Фаза 5: Рекомендуемые сервисы

- [ ] Scheduling Service
- [ ] Consultation Service
- [ ] Integration Service
- [ ] File Service

### ✅ Фаза 6: CRM панель

- [ ] CRM Service
- [ ] Workflow Service
- [ ] Admin Service
- [ ] Frontend CRM панели

### ✅ Фаза 7: Синхронизация

- [ ] Event-Driven Architecture
- [ ] Message Broker
- [ ] Event handlers

---

## 🔗 Ключевые взаимосвязи

### Порядок создания заявки (Order Service):

1. Frontend → Location Service (получить адрес)
2. Frontend → Availability Service (проверить доступность)
3. Frontend → Provider Service (получить тарифы)
4. Frontend → Equipment Service (получить оборудование)
5. Frontend → Order Service (создать заявку):
   - Order Service → Availability Service (проверить доступность)
   - Order Service → Provider Service (получить данные тарифа)
   - Order Service → Equipment Service (получить цены оборудования)
   - Order Service → User Service (получить данные пользователя)
   - Order Service → Location Service (сохранить адрес)
6. Order Service → Notification Service (отправить уведомление)

### Порядок проверки доступности (Availability Service):

1. Frontend → Location Service (получить адрес)
2. Frontend → Availability Service (проверить доступность):
   - Availability Service → Location Service (получить адрес)
   - Availability Service → Provider Service (получить список провайдеров)
   - Availability Service → Integration Service (проверить через внешние API, если есть)
   - Availability Service → Сохранить в кеш

---

## ⚠️ Важные моменты

1. **Нельзя пропускать этапы** - каждый этап зависит от предыдущих
2. **Можно делать параллельно** - независимые сервисы (Location, Provider, Equipment)
3. **Тестировать интеграцию** - после каждого сервиса проверять связь с другими
4. **Миграции БД** - всегда создавать миграции, не изменять БД напрямую
5. **Переменные окружения** - все URL сервисов через переменные окружения
6. **Обработка ошибок** - все HTTP запросы между сервисами должны обрабатывать ошибки

---
