# Структура проекта ГИГАПОИСК

## 📁 Общая структура

```
next/
├── services/              # Микросервисы (Backend)
├── frontend/              # Next.js приложение (Frontend + API Gateway)
├── docs/                  # Документация
├── docker-compose.yml     # Оркестрация всех сервисов
└── README.md              # Основная документация
```

---

## 🔧 Микросервисы (services/)

### ✅ Реализованные сервисы

#### 1. **auth-service** (порт 3001)

- **Назначение:** Аутентификация и авторизация (JWT) - **только для админов/менеджеров**
- **БД:** `auth_db` (порт 5432)
- **Статус:** ✅ Полностью реализован
- **Модели:** User, RefreshToken, VerificationCode
- **API:** `/api/auth/*` (register, login, refresh, logout, verify)
- **Особенности:** Обычные пользователи не регистрируются. Регистрация нужна только для админов и менеджеров системы.

#### 2. **user-service** (порт 3002)

- **Назначение:** Управление профилями операторов/админов (только для CRM панели)
- **БД:** `user_db` (порт 5433)
- **Статус:** ✅ Полностью реализован
- **Модели:** UserProfile (только для операторов, userId не null)
- **API:**
  - `GET /api/users/profile` - получить свой профиль оператора (требует авторизации)
- **Особенности:** 
  - Только для операторов/админов CRM панели
  - Обычные пользователи НЕ используют User Service
  - Данные обычных пользователей хранятся только в Order Service
  - Оператор видит данные пользователя из заявки в CRM
  - Только оператор может изменять данные через CRM панель

#### 3. **provider-service** (порт 3003)

- **Назначение:** Управление провайдерами, тарифами и покрытием
- **БД:** `provider_db` (порт 5434)
- **Статус:** ✅ Полностью реализован
- **Модели:** Provider, Tariff, Coverage, ServiceType
- **API:** `/api/providers/*`, `/api/tariffs/*`, `/api/coverage/*`

#### 4. **order-service** (порт 3004)

- **Назначение:** Обработка заявок на подключение
- **БД:** `orders_db` (порт 5435)
- **Статус:** ✅ Полностью реализован (переименован из application-service)
- **Модели:** Order
- **API:** `/api/orders/*` (создание, получение, обновление статуса, назначение менеджера)
- **Особенности:** Заявки можно создавать без авторизации

### 🆕 Новые сервисы (скелет создан)

#### 5. **location-service** (порт 3005)

- **Назначение:** Управление адресами (регионы, города, типы улиц, улицы/проспекты/переулки, дома, квартиры)
- **БД:** `locations_db` (порт 5436)
- **Статус:** 🔨 Скелет создан, запускается, требует реализации функциональности
- **Модели:** (планируется) Region, City, StreetType, Street, Building, Apartment
- **StreetType:** улица, проспект, переулок, бульвар, площадь, набережная, шоссе, тупик и т.д.
- **API:** Пока только `/health` (health check)
- **Структура:**
  ```
  location-service/
  ├── src/
  │   ├── config/          # Конфигурация БД (database.ts, database.js)
  │   ├── controllers/     # Контроллеры (пусто)
  │   ├── models/          # Модели Sequelize (пусто)
  │   ├── routes/          # Express маршруты (пусто)
  │   ├── services/        # Бизнес-логика (пусто)
  │   ├── middleware/      # Middleware (errorHandler)
  │   ├── migrations/      # Миграции БД (пусто)
  │   ├── seeders/         # Сидеры БД (пусто)
  │   ├── utils/           # Утилиты (logger)
  │   └── index.ts         # Точка входа (Express сервер с health check)
  ├── .dockerignore
  ├── .sequelizerc
  ├── package.json
  ├── tsconfig.json
  └── Dockerfile
  ```

#### 6. **availability-service** (порт 3006)

- **Назначение:** Проверка доступности провайдеров по адресу
- **БД:** `availability_db` (порт 5437)
- **Статус:** 🔨 Скелет создан, запускается, требует реализации функциональности
- **Зависимости:** location-service, provider-service
- **API:** Пока только `/health` (health check)
- **Структура:** Аналогична location-service (controllers/, models/, routes/, services/, migrations/, seeders/)

#### 7. **equipment-service** (порт 3007)

- **Назначение:** Управление оборудованием (роутеры, ТВ-приставки, SIM-карты)
- **БД:** `equipment_db` (порт 5438)
- **Статус:** 🔨 Скелет создан, запускается, требует реализации функциональности
- **Зависимости:** provider-service
- **API:** Пока только `/health` (health check)
- **Структура:** Аналогична location-service (controllers/, models/, routes/, services/, migrations/, seeders/)

#### 8. **notification-service** (порт 3008)

- **Назначение:** Отправка уведомлений (email, SMS)
- **БД:** `notifications_db` (порт 5439, опционально)
- **Статус:** 🔨 Скелет создан, запускается, требует реализации функциональности
- **Зависимости:** order-service
- **API:** Пока только `/health` (health check)
- **Особенности:** Может работать без БД (опционально)
- **Зависимости npm:** nodemailer
- **Структура:** Аналогична location-service (controllers/, models/, routes/, services/, migrations/, seeders/)

---

## 🎨 Frontend (frontend/)

### Структура

```
frontend/
├── src/
│   └── app/
│       ├── api/              # API Gateway routes
│       │   ├── auth/         # Прокси к auth-service
│       │   ├── providers/    # Прокси к provider-service
│       │   ├── tariffs/      # Прокси к provider-service
│       │   └── orders/       # Прокси к order-service
│       ├── page.tsx          # Главная страница
│       └── layout.tsx        # Layout приложения
├── package.json
├── tsconfig.json
└── Dockerfile
```

### Статус

- ✅ Базовый Next.js проект настроен
- ✅ API routes для основных сервисов настроены (auth, providers, tariffs, orders)
- ⚠️ Требуется добавить API routes для новых сервисов:
  - `/api/locations/*` → location-service
  - `/api/availability/*` → availability-service
  - `/api/equipment/*` → equipment-service
  - `/api/notifications/*` → notification-service

---

## 🗄️ Базы данных

Все БД настроены в `docker-compose.yml`:

| Сервис               | БД               | Порт | Статус |
| -------------------- | ---------------- | ---- | ------ |
| auth-service         | auth_db          | 5432 | ✅     |
| user-service         | user_db          | 5433 | ✅     |
| provider-service     | provider_db      | 5434 | ✅     |
| order-service        | orders_db        | 5435 | ✅     |
| location-service     | locations_db     | 5436 | ✅     |
| availability-service | availability_db  | 5437 | ✅     |
| equipment-service    | equipment_db     | 5438 | ✅     |
| notification-service | notifications_db | 5439 | ✅     |

---

## 📋 Стандартная структура сервиса

Каждый сервис имеет следующую структуру:

```
service-name/
├── .dockerignore          # Исключения для Docker
├── .sequelizerc           # Конфигурация Sequelize CLI (указывает на database.js)
├── Dockerfile             # Docker образ (без китайского зеркала npm)
├── package.json           # Зависимости и скрипты
├── tsconfig.json          # Конфигурация TypeScript
└── src/
    ├── config/
    │   ├── database.ts    # Sequelize конфигурация (TypeScript) - для кода приложения
    │   └── database.js    # Sequelize конфигурация (JavaScript) - для Sequelize CLI
    ├── controllers/       # Контроллеры (обработка HTTP запросов)
    ├── models/            # Модели Sequelize
    ├── routes/            # Express маршруты
    ├── services/          # Бизнес-логика
    ├── middleware/        # Express middleware
    │   └── errorHandler.ts
    ├── migrations/        # Миграции БД
    ├── seeders/           # Сидеры БД (опционально)
    ├── utils/             # Утилиты
    │   └── logger.ts      # Winston logger
    └── index.ts           # Точка входа (Express сервер с health check)
```

### Важные детали:

- **database.ts и database.js:** Оба файла необходимы:
  - `database.ts` - используется в TypeScript коде приложения
  - `database.js` - требуется Sequelize CLI для выполнения миграций (`.sequelizerc` указывает на него)
- **Dockerfile:** Использует официальный npm registry (китайское зеркало удалено)
- **Health check:** Каждый сервис имеет endpoint `/health` для проверки работоспособности

---

## 🚀 Порты сервисов

| Сервис               | Порт | URL                   |
| -------------------- | ---- | --------------------- |
| Frontend             | 3000 | http://localhost:3000 |
| auth-service         | 3001 | http://localhost:3001 |
| user-service         | 3002 | http://localhost:3002 |
| provider-service     | 3003 | http://localhost:3003 |
| order-service        | 3004 | http://localhost:3004 |
| location-service     | 3005 | http://localhost:3005 |
| availability-service | 3006 | http://localhost:3006 |
| equipment-service    | 3007 | http://localhost:3007 |
| notification-service | 3008 | http://localhost:3008 |

---

## 📊 Статус реализации

### ✅ ФАЗА 1: Инфраструктура - ВЫПОЛНЕНО

- [x] Переименован application-service → order-service
- [x] Удалена папка application-service
- [x] Создана структура location-service (с папками controllers/, models/, routes/, services/, migrations/, seeders/)
- [x] Создана структура availability-service (с папками controllers/, models/, routes/, services/, migrations/, seeders/)
- [x] Создана структура equipment-service (с папками controllers/, models/, routes/, services/, migrations/, seeders/)
- [x] Создана структура notification-service (с папками controllers/, models/, routes/, services/, migrations/, seeders/)
- [x] Обновлен docker-compose.yml (БД + сервисы)
- [x] Настроены database.ts и database.js во всех сервисах
- [x] Убрано китайское зеркало npm из всех Dockerfile
- [x] Все сервисы запускаются и имеют health check endpoint
- [x] Обновлен Frontend API Gateway (orders вместо applications)

### 🔄 ФАЗА 2: Независимые сервисы - В РАБОТЕ (20% завершено)

- [x] Location Service (модели, миграции, API) ✅ **ЗАВЕРШЕНО**
  - [x] Модели Sequelize: Region, City, StreetType, Street, Building, Apartment
  - [x] Миграции БД (6 миграций созданы и применены)
  - [x] Контроллеры: 8 endpoints реализованы
    - [x] GET /api/locations/regions
    - [x] GET /api/locations/cities?region_id={id}
    - [x] GET /api/locations/street-types
    - [x] GET /api/locations/streets?city_id={id}
    - [x] GET /api/locations/buildings?street_id={id}
    - [x] GET /api/locations/apartments?building_id={id}
    - [x] GET /api/locations/search?q={query}
    - [x] GET /api/locations/autocomplete?q={query}
  - [x] Сервисы: LocationService, GeocoderService
  - [x] Интеграция с Яндекс Геокодером API (ключ настроен)
  - [x] API routes в Next.js: frontend/src/app/api/locations/route.ts
  - [x] Типизация TypeScript исправлена
- [ ] Provider Service (доработать фильтрацию/сортировку)
- [ ] Equipment Service (модели, миграции, API)
- [ ] Проверить Auth Service
- [ ] Проверить User Service

### ✅ ФАЗА 3: Зависимые сервисы - ЗАВЕРШЕНО (100%)

- [x] Availability Service ✅ **ЗАВЕРШЕНО**
- [x] Order Service (доработка) ✅ **ЗАВЕРШЕНО**
- [x] Notification Service ✅ **ЗАВЕРШЕНО**

---

## 🔗 Зависимости между сервисами

```
API Gateway (Next.js)
    │
    ├─→ Auth Service ──┐
    │                  │
    ├─→ User Service ──┼─→ Order Service ──┐
    │                  │                    │
    ├─→ Location Service ──┐               │
    │                      │               │
    ├─→ Provider Service ──┼─→ Availability Service ──┼─→ Order Service
    │                      │               │
    ├─→ Equipment Service ─┘               │
    │                                      │
    └─→ Notification Service ←─────────────┘
```

---

## 📝 Следующие шаги

1. **Реализовать Location Service** (Фаза 2, Шаг 2.1)

   - Создать модели: Region, City, StreetType, Street, Building, Apartment
   - StreetType: улица, проспект, переулок, бульвар, площадь, набережная, шоссе, тупик и т.д.
   - Написать миграции
   - Реализовать контроллеры и API

2. **Обновить User Service** (Фаза 2, Шаг 2.5) ✅

   - ✅ Добавлены поля адреса в UserProfile:
     - `entrance` (подъезд)
     - `floor` (этаж)
     - `intercomCode` (код домофона)
     - `addressComment` (комментарий к адресу)
   - ✅ Создана миграция для новых полей

3. **Доработать Provider Service** (Фаза 2, Шаг 2.2)

   - Добавить фильтрацию и сортировку тарифов

4. **Реализовать Equipment Service** (Фаза 2, Шаг 2.3)

   - Создать модели: EquipmentType, Equipment
   - Написать миграции
   - Реализовать API

5. ✅ **Реализовать Availability Service** (Фаза 3, Шаг 3.1) - ЗАВЕРШЕНО

   - ✅ Созданы модели: TechnicalAccess, AvailabilityCache
   - ✅ Интегрирован с Location и Provider сервисами
   - ✅ Реализовано кеширование результатов

6. ✅ **Доработать Order Service** (Фаза 3, Шаг 3.2) - ЗАВЕРШЕНО
   - ✅ Обновлены модели под новую структуру
   - ✅ Интегрирован с другими сервисами
   - ✅ Реализован расчет стоимости

7. ✅ **Реализовать Notification Service** (Фаза 3, Шаг 3.3) - ЗАВЕРШЕНО
   - ✅ Создана модель: Notification
   - ✅ Интегрирован с Order Service
   - ✅ Реализованы шаблоны уведомлений

---

---

## 🔍 Текущее состояние сервисов

### ✅ Полностью рабочие сервисы:

- **auth-service** - регистрация, логин, JWT токены
- **user-service** - профили пользователей с адресными полями
- **provider-service** - провайдеры, тарифы, покрытие
- **order-service** - создание и управление заявками

### 🔨 Скелеты (запускаются, но без функциональности):

- **location-service** - только health check, модели и API не реализованы
- **availability-service** - только health check, модели и API не реализованы
- **equipment-service** - только health check, модели и API не реализованы
- **notification-service** - только health check, модели и API не реализованы

### 🧪 Как проверить работу сервисов:

```bash
# Запустить все сервисы
docker-compose up --build

# Проверить health check всех сервисов
curl http://localhost:3001/health  # auth-service
curl http://localhost:3002/health  # user-service
curl http://localhost:3003/health  # provider-service
curl http://localhost:3004/health  # order-service
curl http://localhost:3005/health  # location-service
curl http://localhost:3006/health  # availability-service
curl http://localhost:3007/health  # equipment-service
curl http://localhost:3008/health  # notification-service
```

---

**Последнее обновление:** Фаза 1 завершена ✅, все сервисы запускаются
