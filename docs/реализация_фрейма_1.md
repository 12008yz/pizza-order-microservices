# Реализация Фрейма 1 - Форма ввода адреса

## ✅ Что было реализовано

### 1. Управление состоянием (AddressContext)
- **Файл:** `frontend/src/contexts/AddressContext.tsx`
- **Функционал:**
  - Централизованное управление состоянием адреса
  - Валидация формы
  - Методы обновления полей с автоматической очисткой зависимых полей
  - Типы подключения: `apartment`, `private`, `office`

### 2. Компоненты

#### AddressAutocomplete
- **Файл:** `frontend/src/components/AddressAutocomplete.tsx`
- **Функционал:**
  - Автодополнение для города, улицы и номера дома
  - Debounce 300ms для оптимизации запросов
  - Каскадные зависимости (улица зависит от города, дом от улицы)
  - Интеграция с `/api/locations/autocomplete` и `/api/locations/buildings`
  - Визуальная индикация загрузки

#### ConnectionTypeModal
- **Файл:** `frontend/src/components/ConnectionTypeModal.tsx`
- **Функционал:**
  - Модальное окно выбора типа подключения
  - Радио-кнопки для выбора: квартира, частный сектор, офис
  - Валидация выбора перед переходом

#### PrivacyConsent
- **Файл:** `frontend/src/components/PrivacyConsent.tsx`
- **Функционал:**
  - Чекбокс согласия с политикой конфиденциальности
  - Ссылка на политику конфиденциальности
  - Валидация и отображение ошибок

#### AddressFormPage (обновлен)
- **Файл:** `frontend/src/components/AddressFormPage.tsx`
- **Функционал:**
  - Интеграция всех компонентов
  - Валидация формы перед отправкой
  - Проверка доступности провайдеров через API
  - Баннер cookies с авто-закрытием через 7 секунд
  - Переход на страницу результатов после успешной проверки

### 3. Сервисы

#### LocationsService (обновлен)
- **Файл:** `frontend/src/services/locations.service.ts`
- **Добавлено:**
  - Метод `getBuildings()` для получения списка домов по улице
  - Метод `searchAddress()` для поиска адреса

#### AvailabilityService (обновлен)
- **Файл:** `frontend/src/services/availability.service.ts`
- **Изменено:**
  - Метод `checkAvailability()` теперь использует POST запрос
  - Принимает параметры: `city`, `street`, `house`, `buildingId`, `apartmentId`

### 4. Страница результатов
- **Файл:** `frontend/src/app/providers/page.tsx`
- **Функционал:**
  - Отображение списка доступных провайдеров
  - Получение данных из sessionStorage
  - Переход к детальной странице провайдера

## 🔄 Поток данных

1. **Выбор типа подключения** → Сохранение в контекст
2. **Ввод города** → Автодополнение → Сохранение `cityId`, `city`, `regionId`
3. **Ввод улицы** → Автодополнение (только после выбора города) → Сохранение `streetId`, `street`
4. **Ввод номера дома** → Автодополнение (только после выбора улицы) → Сохранение `buildingId`, `houseNumber`
5. **Согласие с политикой** → Сохранение `privacyConsent`
6. **Нажатие "Показать всех операторов"** → Валидация → POST `/api/availability/check` → Переход на `/providers`

## 📡 API Endpoints

### Location Service
- `GET /api/locations?endpoint=autocomplete&q={query}&cityId={id}` - Автодополнение адреса
- `GET /api/locations?endpoint=buildings&street_id={id}` - Список домов по улице
- `GET /api/locations?endpoint=search&q={query}` - Поиск адреса

### Availability Service
- `POST /api/availability/check` - Проверка доступности провайдеров
  ```json
  {
    "city": "Москва",
    "street": "Кремлевская наб.",
    "house": "9",
    "buildingId": 123,
    "apartmentId": 456
  }
  ```

## 🎨 UI/UX Особенности

1. **Каскадные зависимости:**
   - Поле "Улица" неактивно до выбора города
   - Поле "Номер дома" неактивно до выбора улицы

2. **Валидация:**
   - Визуальная индикация ошибок (красные границы)
   - Сообщения об ошибках под полями
   - Кнопка "Показать всех операторов" неактивна до заполнения всех полей

3. **Автодополнение:**
   - Минимум 2 символа для запроса
   - Debounce 300ms
   - Индикатор загрузки
   - Закрытие при клике вне компонента

4. **Баннер cookies:**
   - Автоматическое закрытие через 7 секунд
   - Можно закрыть вручную кликом вне баннера

## 🔧 Технические детали

### Используемые технологии
- React Context API для управления состоянием
- Next.js App Router
- TypeScript для типизации
- Tailwind CSS для стилей (inline styles в некоторых местах для точного соответствия макету)

### Структура данных AddressData
```typescript
{
  connectionType: 'apartment' | 'private' | 'office' | '';
  regionId?: number;
  cityId?: number;
  city?: string;
  streetId?: number;
  street?: string;
  buildingId?: number;
  houseNumber?: string;
  apartmentId?: number;
  privacyConsent: boolean;
  errors: {
    connectionType?: string;
    city?: string;
    street?: string;
    houseNumber?: string;
    privacyConsent?: string;
  };
}
```

## 📝 Следующие шаги

1. ✅ Реализована форма ввода адреса
2. ✅ Реализована проверка доступности провайдеров
3. ⏳ Создать страницу детального просмотра провайдера (`/providers/[id]`)
4. ⏳ Создать страницу тарифов провайдера
5. ⏳ Реализовать создание заявки на подключение

## 🐛 Известные проблемы

- Нет обработки случая, когда API возвращает пустой массив провайдеров (частично обработано на странице providers)
- Нет обработки таймаутов при запросах к API
- Баннер cookies не имеет кнопки закрытия (только авто-закрытие)

## 📦 Зависимости

Все зависимости уже установлены в проекте:
- `react`, `react-dom`
- `next`
- `axios` (через apiClient)
- `typescript`
